package classes.repositories;

public class RoomRepository implements IRoomRepository {
    @Override
    public boolean createRoom(int playerID, String name, String password) {
        return false;
    }
}
